[package-test]
  command = 'Rscript'
  args = ['--no-save', '--no-restore', '-e devtools::test()']i
  isBackground = true

  [package-build]
    command = 'R'
    args = ['CMD', 'INSTALL', '--no-multiarch', '--with-keep.source', '.']
    isBackground = true
